# Prefer pointing to your API Gateway route (recommended):
PRODUCT_BASE_URL=http://gateway:8080/api/products
# For direct local testing, you can hit the service directly:
# PRODUCT_BASE_URL=http://localhost:8002/products

# Relational DB (SQLite by default; swap to Postgres if desired)
DATABASE_URL=sqlite:///./supplier.db

# Logging & HTTP settings
LOG_LEVEL=INFO
HTTP_TIMEOUT=5
HTTP_RETRIES=2
