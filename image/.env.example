# Prefer routing through your API Gateway:
PRODUCT_BASE_URL=http://gateway:8080/api/products
# For direct local testing (without gateway), you can use:
# PRODUCT_BASE_URL=http://localhost:8002/products

DATABASE_URL=sqlite:///./image.db
LOG_LEVEL=INFO

HTTP_TIMEOUT=5
HTTP_RETRIES=2
